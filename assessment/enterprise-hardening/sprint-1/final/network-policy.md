Policy is immutable per crawl. Read-only methods are allowed. Mutation,
destructive, recovery, account, logout, upload, and GraphQL rules are evaluated
from the run configuration. The fixture observed zero background mutation
requests at the server.
